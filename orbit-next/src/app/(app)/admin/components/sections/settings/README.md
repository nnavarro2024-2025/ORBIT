# Settings Section

Re-export of legacy `SettingsSection` for backward compatibility. Tabs split into individual files under `tabs/`.

Exports:
- `SettingsSection`
- `SETTINGS_TABS`
- Individual tab constants (e.g. `SETTINGS_TAB_FACILITIES`).
